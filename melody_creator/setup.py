# A setup.py file gives the pip dependency manager (and PyPI) information about this library: most importantly, the
# version of this library.
from setuptools import setup

setup(
    name='melody_creator',
    version='1.0.0',
    packages=['melody_creator'],
    url='',
    license='',
    author='Ivan Post',
    author_email='',
    description=''
)
